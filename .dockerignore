node_modules
.next
.git
.github
.vercel
*.md
LICENSE
scripts/
# Never bake secrets or local runtime data into the image.
# NOTE: keep data/mock/ (build-time demo data imported by lib/demo-data.ts);
# only the top-level runtime store JSON is excluded.
.env
.env.*
*.env
data/*.json
templates/
